#Ejercicio Alumno que aprobó o no aprobó

#primero se crea la clase alumno e inicimos el atributo 

class Alumno:
    def inicializar(self,nombre,nota):
        self.nombre=nombre
        self.nota=nota